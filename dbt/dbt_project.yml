name: 'vivacolombia_dbt'
version: '1.0.0'
config-version: 2

profile: 'vivacolombia'

model-paths: ["models"]
test-paths:  ["tests"]
seed-paths:  ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

# Materializaciones por capa
models:
  vivacolombia_dbt:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
